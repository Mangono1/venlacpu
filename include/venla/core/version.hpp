#pragma once

namespace venla {

constexpr int VERSION_MAJOR = 2;
constexpr int VERSION_MINOR = 2;
constexpr int VERSION_PATCH = 1;

constexpr const char* VERSION_STRING = "2.2.1";

const char* version();

} // namespace venla
