#pragma once

namespace venla {

constexpr int VERSION_MAJOR = 2;
constexpr int VERSION_MINOR = 3;
constexpr int VERSION_PATCH = 2;

constexpr const char* VERSION_STRING = "3.1.1";

const char* version();

} // namespace venla
